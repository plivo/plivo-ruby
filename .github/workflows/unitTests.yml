name: UnitTests

on:
  push:
    branches: [ unitTests ]
  pull_request:
    branches: [ master ]
    
jobs:
  build:
    name: UnitTests
    runs-on: ubuntu-latest
    strategy:
      matrix:
         ruby-version: [ 'ruby-head', '2.3', '2.2' ,'2.1' , '2.0' ]

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Ruby ${{ matrix.ruby-version }}
        uses: actions/setup-ruby
        with:
          ruby-version: ${{ matrix.ruby-version }}
      - name: Dependencies Installation
        run: |
          bundle install --without development
      - name: Run Tests
        run: |
          ruby --version
          bundle exec rake
